import {makeStyles} from "@material-ui/core";

export default makeStyles(() => ({
    income: {
        borderBottom: "15px solid rgba(76, 175, 80, .8)"
    },
    expense: {
        borderBottom: "15px solid rgba(244, 67, 54, .8)"
    }
}));